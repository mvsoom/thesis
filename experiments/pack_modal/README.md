# pack_modal

GPU = 20x acceleration, but still takes O(1) hour

Fit PACK to 4 modal LF waveforms

- Let center be in [0, T]
- We fix t1 := 0 and t2 := tc in [0, T]
- We fix sigma_c := 1, otherwise sigma_a nonidentifiable
